services:
  db:
    container_name: parkspace_db
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - db_data_parkspace:/var/lib/postgresql/data

volumes:
  db_data_parkspace:
